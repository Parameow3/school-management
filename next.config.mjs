// next.config.mjs
const nextConfig = {
    reactStrictMode: true, // This is optional, but useful for debugging
    images: {
      domains: ['api.adskh.com'], // Allow images from localhost API domain
    },
  };
  
  export default nextConfig;
  
